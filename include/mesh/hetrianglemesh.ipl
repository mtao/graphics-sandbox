#ifndef HETRIANGLEMESH_IPL
#define HETRIANGLEMESH_IPL
#ifndef HETRIANGLEMESH_H
#include "hetrianglemesh.h"
#endif


namespace mtao {
template <typename Scalar>
inline auto HETriangleMesh<Scalar>::vertex(size_t idx) -> Vec3& {
    return m_vertices[idx];
}
template <typename Scalar>
inline auto HETriangleMesh<Scalar>::vertex(size_t idx) const -> const Vec3&{
    return m_vertices[idx];
}
}





#endif
